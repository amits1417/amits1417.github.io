<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Your Name – Portfolio 2026</title>

  <!-- GOOGLE FONT -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">

  <style>
    /* ---------- RESET ---------- */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: #0e0e0e;
      color: #ffffff;
      line-height: 1.6;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    img {
      width: 100%;
      display: block;
      border-radius: 14px;
    }

    /* ---------- NAVBAR ---------- */
    header {
      position: fixed;
      top: 0;
      width: 100%;
      padding: 20px 60px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: rgba(14,14,14,0.8);
      backdrop-filter: blur(10px);
      z-index: 1000;
    }

    header h1 {
      font-size: 1.2rem;
      font-weight: 600;
      letter-spacing: 1px;
    }

    nav a {
      margin-left: 30px;
      font-size: 0.9rem;
      opacity: 0.7;
      transition: 0.3s;
    }

    nav a:hover {
      opacity: 1;
    }

    /* ---------- HERO ---------- */
    .hero {
      height: 100vh;
      display: flex;
      align-items: center;
      padding: 0 60px;
    }

    .hero-content {
      max-width: 900px;
    }

    .hero h2 {
      font-size: clamp(2.5rem, 6vw, 5rem);
      font-weight: 700;
      line-height: 1.1;
    }

    .hero p {
      margin-top: 20px;
      font-size: 1.1rem;
      opacity: 0.7;
      max-width: 600px;
    }

    /* ---------- SECTION ---------- */
    section {
      padding: 120px 60px;
    }

    section h3 {
      font-size: 2rem;
      margin-bottom: 40px;
    }

    /* ---------- PROJECT GRID ---------- */
    .projects-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 30px;
    }

    .project-card {
      cursor: pointer;
      transition: transform 0.4s ease;
    }

    .project-card:hover {
      transform: translateY(-10px);
    }

    .project-info {
      margin-top: 15px;
    }

    .project-info h4 {
      font-size: 1.1rem;
      font-weight: 600;
    }

    .project-info span {
      font-size: 0.85rem;
      opacity: 0.6;
    }

    /* ---------- ABOUT ---------- */
    .about {
      max-width: 900px;
    }

    .about p {
      font-size: 1.1rem;
      opacity: 0.8;
    }

    /* ---------- CONTACT ---------- */
    .contact p {
      font-size: 1.1rem;
      opacity: 0.8;
    }

    .contact a {
      display: inline-block;
      margin-top: 20px;
      font-weight: 600;
      border-bottom: 1px solid white;
    }

    /* ---------- FOOTER ---------- */
    footer {
      text-align: center;
      padding: 40px;
      opacity: 0.5;
      font-size: 0.85rem;
    }

    /* ---------- RESPONSIVE ---------- */
    @media (max-width: 768px) {
      header {
        padding: 20px 30px;
      }
      .hero, section {
        padding: 100px 30px;
      }
      nav a {
        margin-left: 15px;
      }
    }
  </style>
</head>

<body>

  <!-- NAV -->
  <header>
    <h1>YourName</h1>
    <nav>
      <a href="#work">Work</a>
      <a href="#about">About</a>
      <a href="#contact">Contact</a>
    </nav>
  </header>

  <!-- HERO -->
  <div class="hero">
    <div class="hero-content">
      <h2>Creative Developer & Visual Designer</h2>
      <p>
        I design modern digital experiences with a focus on minimalism,
        motion, and strong visual identity.
      </p>
    </div>
  </div>

  <!-- WORK -->
  <section id="work">
    <h3>Selected Work</h3>
    <div class="projects-grid">

      <div class="project-card">
        <img src="https://images.unsplash.com/photo-1529333166437-7750a6dd5a70" alt="">
        <div class="project-info">
          <h4>Portfolio 2026</h4>
          <span>Branding / UI</span>
        </div>
      </div>

      <div class="project-card">
        <img src="https://images.unsplash.com/photo-1522199710521-72d69614c702" alt="">
        <div class="project-info">
          <h4>Creative Website</h4>
          <span>Web Design</span>
        </div>
      </div>

      <div class="project-card">
        <img src="https://images.unsplash.com/photo-1517694712202-14dd9538aa97" alt="">
        <div class="project-info">
          <h4>Visual Identity</h4>
          <span>Design System</span>
        </div>
      </div>

    </div>
  </section>

  <!-- ABOUT -->
  <section id="about">
    <h3>About Me</h3>
    <div class="about">
      <p>
        I’m a designer and developer focused on building visually
        compelling digital products. Inspired by modern portfolios on
        Behance, I blend strong layouts, typography, and smooth interactions
        into my work.
      </p>
    </div>
  </section>

  <!-- CONTACT -->
  <section id="contact">
    <h3>Contact</h3>
    <div class="contact">
      <p>Let’s work together.</p>
      <a href="mailto:your@email.com">your@email.com</a>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    © 2026 Your Name — Portfolio
  </footer>

</body>
</html>
